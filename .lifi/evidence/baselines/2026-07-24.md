# Little Fight NYC Quality Baseline — 2026-07-24

Status: **CONDITIONAL BASELINE — PRODUCTION ADDENDUM 2026-07-25**. The
product-quality spine is live, but provider, owner, research, claims,
social-preview, dependency, and full evidence gates remain open.

## Source of truth

- Repository: `/Users/davidmarsh/Code/LiFi NYC/Little Fight NYC Business/Website/littlefightnyc-website`
- Remote: `https://github.com/omgitsthedm/littlefightnyc-website.git`
- Branch: `main`
- Netlify site: `0907d8fe-7018-48db-a6be-1f906e4b2619`
- Production: `https://littlefightnyc.com`
- Released application revision: `d02b93549ad79cc2a904f3220d2a06b1643f114a`
- Ready production deploy: `6a642637809f6e0008ac8831`
- Previous rollback revision: `df7c90ded191d909648ed86401fc5816809648ec`
- Deploy path: GitHub `main` → Netlify auto-build → auto-publish
- Central Quality Charter: **absent** at `/Users/davidmarsh/Code/LiFi NYC/agency-ops/quality/QUALITY-CHARTER.md`; no version was inferred.
- This evidence file and its three sibling `.lifi` records are the scope of this baseline. Other working-tree changes, including `SITE-REINVENTION-DOSSIER.md`, are outside this subtask and are not attributed here.

## Classification

**SERVICE-ENABLED / MODERATE.** Most routes are public marketing, proof, answers, and static experiments. The risk rises because Netlify Forms receives contact data and the Website Audit Lab accepts a public URL plus email, starts one-time-authorized background work, stores job/report/engagement state in Netlify Blobs, and can send data to PageSpeed, Anthropic, Gmail SMTP, and optional Notion. There are no verified user accounts, payments, or authenticated multi-tenant roles.

## Inventory snapshot

- Generated SPA catalog: **200 routes** — home 1, services 5, case studies 10, answers 27, journal 37, areas 91, industries 7, glossary 7, studio 3, other 12; **73 noindex**.
- Lab manifest: version 3, **4 suites / 11 concepts**.
- Website Audit: **8 deployable surfaces** — request, background, status, report, OG, engagement, views, and scheduled cleanup — plus shared function helpers.
- Dynamic/provider contracts protected in the dead-code ledger: generated route/nav/journal data, public/Lab/Audit/brand-kit files, Netlify Forms registration, redirects and crawler files, Spanish/Chinese routes, VERA proxy paths, assets, function helpers, and the passive service-worker/chunk-retry lifecycle.
- Audit stores: `audit-status`, `audit-jobs`, `rate-limits`, `audit-pages`, `audit-meta`, `audit-views`, and `audit-engagement`.
- Scheduled cleanup: `0 3 * * *`; report lifetime is 30 days.

## Declared quality commands

Implemented in the current working tree:

```text
npm run quality:fast
npm run quality:full
npm run quality:release
npm run quality:live
npm run quality:maintenance
```

The full tier includes representative Playwright route contracts across desktop/mobile Chromium, desktop Firefox, and mobile WebKit; axe WCAG A/AA checks; first-party console/page errors; horizontal overflow; and reduced-motion behavior. The release tier checks revision-bound artifacts and required public recovery/identity files. The live tier checks production route/asset/metadata/revision behavior. Visual-regression baselines, provider-contract tests, unit/component tests, numeric field-performance enforcement, and a retained CI artifact policy remain open under `QSP-002`.

## Artifact snapshot

The initial `app/dist` snapshot was inspected by the `.lifi` inventory lane.
The integrated release candidate was subsequently built and passed `quality:full`
under Node 24.18.0/npm 10.9.8, including all repository audits and **32/32**
Playwright checks across desktop/mobile Chromium, desktop Firefox, and mobile
WebKit. The same lane passed again after clean root and app `npm ci` installs.
The clean release candidate subsequently passed `quality:release`, including
its revision-matched release artifact and public recovery/identity checks,
before becoming production application release `d02b935`.

- Generated HTML files: **267**
- Main entry JavaScript: **350,731 bytes**
- Main entry CSS: **123,363 bytes**
- Largest ordinary lazy JavaScript chunk: **149,908 bytes** (`leaflet-src`)
- Largest ordinary responsive image observed: **428,474 bytes**
- Lab thumbnail cap in the repository audit: **120,000 bytes**
- Current `app/dist/assets` contains **652,924 bytes of font files**, including duplicate formats/subsets and the dedicated `assets/fonts` set; this exceeds the manifest's 130,000-byte target if enforced as total emitted font bytes and needs an explicit counting rule/remediation before numeric enforcement.

Targets declared in `.lifi/quality.yml`: field p75 LCP ≤ 2.5 s, INP ≤ 200 ms, CLS ≤ 0.1; median mobile Lighthouse ≥ 90 over three runs; 60 fps target with simplification below 50 fps; zero optional analytics requests before consent; entry JS ≤ 375,000 bytes; entry CSS ≤ 135,000 bytes; largest lazy JS ≤ 165,000 bytes; ordinary responsive image ≤ 450,000 bytes; social card ≤ 600,000 bytes.

## Open gates

- `QSP-001`: central Quality Charter and approver absent.
- `QSP-002`: quality tiers exist; visual, provider, component, performance, and retained-evidence coverage is incomplete.
- `QSP-003`: **closed for release `d02b935`**. Node 24 is pinned, clean
  installs and `quality:release` passed locally, the exact revision reached a
  ready Node 24 Netlify deploy, and revision-matched live checks passed. Reopen
  on runtime or deployment-path change.
- `EXT-001`: production-safe Netlify Forms and Website Audit email delivery proof missing.
- `RES-001`: representative owner comprehension/trust/language research missing.
- `PROOF-001`: per-case quote, result, timing, logo, image, privacy, and link approval inventory missing.
- `OPS-001`: backup owner, funded SLA, maintenance cadence, alert owner, and escalation path missing.
- `EXT-002`: authenticated Netlify, search, business-profile, analytics, SMTP, and optional-provider validation missing.
- `SOCIAL-001`: Meta, LinkedIn, and X debugger checks missing.
- `SEO-001`: long-term decision for the 200-route / 91-area / 73-noindex fanout missing.
- `DEP-001`: high `react-router` advisory `GHSA-qwww-vcr4-c8h2`; the current client SPA appears not to use the affected RSC/action path, but owner review and a compatible non-affected upgrade remain required.
- `DEP-002`: high `brace-expansion` advisories `GHSA-3jxr-9vmj-r5cp` and `GHSA-mh99-v99m-4gvg` in development/tooling transitive paths; clean compatible fixes are available and packages were not changed by this baseline.

## Release boundary

The original baseline collection performed no live/provider mutation, form
submission, credential access, deletion, or deploy. On 2026-07-25, David
explicitly authorized the release: `d02b935` was pushed to `main`, Netlify
deploy `6a642637809f6e0008ac8831` reached ready, `quality:live` passed, all 200
catalog routes and 78 share images passed, and the independent mobile smoke
found no crash or reload. No form or provider-delivery test was submitted.

---

*Amended 2026-07-30: the alert-channel integration named in the original
classification was deleted from the codebase and from the visitor-facing
privacy disclosure. It had never been configured in production. This baseline
is dated evidence, so the amendment is recorded here rather than left silent.*
