{
  "scripts": {
    "preview": "node scripts/preview-server.mjs",
    "build:html": "node build/inject-partials.mjs",
    "build:html:check": "node build/inject-partials.mjs --check"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3",
    "terser": "^5.46.1"
  }
}
